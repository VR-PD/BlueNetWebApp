@model List<VRPD_WebApp.db.GameOverview>

@{
    ViewBag.Title = "Overview";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Overview</h2>

@foreach (var game in Model)
{
    <h3>@game.Title</h3>
    <p>@game.Description</p>
    <img src="@Url.Action("RenderImage", new { id = game.Id })" />
}
